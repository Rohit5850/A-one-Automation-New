"use client";
import { Suspense, useState } from "react";
import { useSearchParams, useRouter } from "next/navigation";
function Form(){const q=useSearchParams();const router=useRouter();const token=q.get("token")||"";const[newPassword,setNew]=useState("");const[confirmPassword,setConfirm]=useState("");const[err,setErr]=useState("");const[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setErr("");const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({token,newPassword,confirmPassword})});const d=await r.json().catch(()=>({}));setLoading(false);if(!r.ok)return setErr(d.error||"Reset failed");router.replace("/login?reset=success");}return <form onSubmit={submit} className="w-full max-w-md bg-white border rounded-2xl p-6 shadow-sm space-y-4"><h1 className="text-xl font-semibold">Reset Password</h1>{err&&<div className="text-sm text-red-700 bg-red-50 p-3 rounded">{err}</div>}<input type="password" required value={newPassword} onChange={e=>setNew(e.target.value)} placeholder="New Password" className="w-full h-12 border rounded-lg px-3"/><input type="password" required value={confirmPassword} onChange={e=>setConfirm(e.target.value)} placeholder="Confirm New Password" className="w-full h-12 border rounded-lg px-3"/><button disabled={loading||!token} className="w-full h-12 bg-indigo-600 text-white rounded-lg disabled:opacity-50">{loading?"Saving...":"Reset Password"}</button></form>}
export default function Page(){return <main className="min-h-screen bg-slate-50 flex items-center justify-center p-5"><Suspense fallback={<p>Loading...</p>}><Form/></Suspense></main>}
