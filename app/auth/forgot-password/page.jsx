"use client";
import { useState } from "react";
import Link from "next/link";
export default function ForgotPassword(){const[email,setEmail]=useState("");const[msg,setMsg]=useState("");const[err,setErr]=useState("");const[loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setErr("");setMsg("");setLoading(true);const r=await fetch("/api/auth/forgot-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({email})});const d=await r.json().catch(()=>({}));setLoading(false);if(!r.ok)return setErr(d.error||"Request failed");setMsg(d.message||"Reset link send ho gaya.");}return <main className="min-h-screen bg-slate-50 flex items-center justify-center p-5"><form onSubmit={submit} className="w-full max-w-md bg-white border rounded-2xl p-6 shadow-sm space-y-4"><h1 className="text-xl font-semibold">Forgot Password</h1><p className="text-sm text-slate-500">Registered email par password reset link bheja jayega.</p>{err&&<div className="text-sm text-red-700 bg-red-50 p-3 rounded">{err}</div>}{msg&&<div className="text-sm text-emerald-700 bg-emerald-50 p-3 rounded">{msg}</div>}<input type="email" required value={email} onChange={e=>setEmail(e.target.value)} placeholder="Registered Email" className="w-full h-12 border rounded-lg px-3"/><button disabled={loading} className="w-full h-12 bg-indigo-600 text-white rounded-lg">{loading?"Sending...":"Send Reset Link"}</button><Link href="/login" className="block text-center text-sm text-indigo-600">Back to Login</Link></form></main>}
